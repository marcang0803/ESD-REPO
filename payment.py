import stripe
import time
import api_ob
from idem_key import generate_key
session_id = ''

def session_time():
    # sets session time and generate idem key
    start = time.time()
    session_id = generate_key

    return start

def provide_payment():
    pass



print(session_time())
