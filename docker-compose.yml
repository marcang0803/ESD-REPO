services:
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: rabbitmq-broker
    ports:
      - "5672:5672"   # The port your Python code will use to connect
      - "15672:15672" # The port for the Web UI